{
  "root": "apps/one-auth",
  "sourceRoot": "apps/one-auth",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/one-auth",
        "outputPath": "dist/apps/one-auth"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "one-auth:build",
        "dev": true,
        "port": 8000
      },
      "configurations": {
        "production": {
          "buildTarget": "one-auth:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "one-auth:build:production"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/one-auth/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
