{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Auth Dev Server",
      "request": "launch",
      "command": "pnpm run dev --port 8000",
      "cwd": "${workspaceFolder}"
    }
  ]
}
